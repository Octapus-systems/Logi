services:
  mongo:
    image: mongo:7
    container_name: logi-mongo
    ports:
      - "27017:27017"
    volumes:
      - logi_mongo_data:/data/db
    restart: unless-stopped

volumes:
  logi_mongo_data:

